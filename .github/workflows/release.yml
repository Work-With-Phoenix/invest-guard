name: Invest Guard

on:
  push:
    branches:
      - main  # Adjust the branch name as needed

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
      
    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: 3.9  # Specify the Python version you're using
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install setuptools wheel twine  

    - name: Build and upload
      env:
        TWINE_USERNAME: __token__  # Make sure to set your PyPI username as a GitHub secret
        TWINE_PASSWORD: pypi-AgEIcHlwaS5vcmcCJGQ3OGU5NWRiLWNlMTgtNDYwYi1hYzEwLWMyN2I2ODBiYWMzOAACKlszLCI2MmViMjY2NS0xNWJlLTRhZjMtODdmMy0xZGM1OGJmNTUyNGIiXQAABiCsdQGtLJyCl1NmEMAcoHvVpqfM6Kz9U_oD1wOV_5YXFg
      run: |
        python setup.py sdist bdist_wheel  
        twine upload --repository-url https://test.pypi.org/legacy/ dist/* 
